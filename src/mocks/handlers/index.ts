/**
 * MSW 핸들러 통합 진입점
 *
 * 새로운 핸들러를 추가할 때 이 파일에서 import하여 handlers 배열에 추가하세요.
 *
 * @example
 * import { authHandlers } from './auth'
 * export const handlers = [...exampleHandlers, ...authHandlers]
 */

import { exampleHandlers } from './example';

export const handlers = [...exampleHandlers];
